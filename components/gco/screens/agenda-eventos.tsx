"use client"

import { CalendarioEventos } from "./calendario"

/** La agenda usa exactamente el mismo calendario compartido.
 * El componente adapta datos y acciones según el rol activo. */
export function AgendaEventos() {
  return <CalendarioEventos />
}
